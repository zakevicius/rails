<% flash.each do |type, msg| %>
  <div class="alert alert-<%= type == 'notice' ? 'warning' : type %> alert-dismissible fade show mt-3" role="alert">
    <%= msg %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>
